enum TextFieldBorder { outlinedBorder, baseBorder, none, forGuestContact }

enum BackgroundGradientPosition { topPosition, centerPosition, endPosition }

enum TicketStatus {
  initial,
  valid,
  invalid,
  duplicate,
  validate,
}

enum TicketHelperStatus {
  paid,
  cancelled,
  rejected,
  unpaid,
  voided,
}

enum ScannerTableHistoryStatus {
  valid,
  double,
  invalid,
  refunded,
  voided,
  validate,
}
